{
  "buildCommand": "CI=false react-scripts build && node scripts/update-sitemap-dates.js",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
